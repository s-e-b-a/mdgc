Transform a Java Swing desktop application into a React frontend using TypeScript, Tailwind CSS and shadcn/ui.
Convert JFrame, JPanel, JTable, dialogs and menus into React pages and reusable components.
Preserve all workflows, validations and screens.
Use React Router, Zustand and React Query.
Generate a Vite project with Dockerfile and build scripts.